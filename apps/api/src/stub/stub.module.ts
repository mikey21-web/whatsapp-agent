import { Module } from '@nestjs/common';

/**
 * All originally-stubbed modules now have real implementations.
 * Keeping this module in the tree as an extension point for any
 * future-phase placeholder routes.
 */
@Module({})
export class StubModule {}
